node_modules
dist
.vite
node_modules/.vite

# Local env / editor
.DS_Store
*.local
.env
.env.*

# Design references may be licensed stock — keep them out of the public repo.
# (Original assets we create live under src/ and are committed.)
design/inspiration/
